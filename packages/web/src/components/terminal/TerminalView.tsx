// Terminal view has been replaced by AgentCanvas + ChatSidebar.
// This stub exists to prevent import errors during the transition.

interface TerminalViewProps {
  agentId: string;
}

export function TerminalView(_props: TerminalViewProps) {
  return null;
}
