version: "3.8"

services:
  pulse:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - pulse-data:/app/data
    env_file:
      - .env
    restart: unless-stopped

volumes:
  pulse-data:
